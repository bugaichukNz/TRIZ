"""Промпты повторной генерации ТП и ФП после отклонения валидатором."""

FP_FORMULA = """Строгие формулы физического противоречия (ФП) — одно предложение, дословно по одному из шаблонов.

A) Статическое ФП (если в задаче НЕТ разных фаз процесса):
«[элемент]: параметр [P] должен быть [X], чтобы [полезная функция A], и должен быть [анти-X], чтобы [полезная функция B].»

B) ФП с разделением во ВРЕМЕНИ (ПРИОРИТЕТ, если в брифе разные фазы — инкубация/эксперимент, сначала/потом, подготовка/работа):
«[элемент]: параметр [P] на фазе [A] должен быть [X], чтобы [полезная функция A], и на фазе [B] должен быть [анти-X], чтобы [полезная функция B].»

Где:
- [элемент] — ОДИН конкретный элемент системы (стакан, реакционная зона, поднос…);
- [P] — ОДИН именованный параметр ЭТОГО элемента (ширина зоны активации, температура внешней поверхности…);
- [X] и [анти-X] — взаимоисключающие значения ОДНОГО параметра [P] (широкий/узкий, горячий/холодный…);
- [A] и [B] — явные названия фаз из брифа (инкубация / эксперимент, загрузка / обработка…).

Пример статического ФП (стаканы):
«Стакан: параметр температура внешней поверхности должен быть низкой, чтобы напиток оставался холодным, и должен быть высокой, чтобы капли воды после мытья быстро испарялись до переноски.»

Пример фазового ФП (микрофлюидика):
«Реакционная зона: параметр ширина зоны активации на фазе инкубации должен быть широким, чтобы обеспечить равномерную подачу реагентов и активацию белка, и на фазе эксперимента должен быть узким, чтобы локализовать наблюдаемый биохимический процесс под микроскопом.»"""

FP_PARAMETER_ENUMERATION = """Алгоритм выбора параметра P (выполни ДО записи ФП):

0. Шаг исключения отвергнутых компонентов:
   Прочитай ideal_final_result и constraints. Определи: какой компонент или подход отвергнут или исключён
   (напр. «без линз», «отказ от линзовой оптики», «без клея»)?
   Параметры этого отвергнутого компонента (преломляющая способность линз, число линз, фокусное расстояние линзы,
   температура клея…) ЗАПРЕЩЕНО выбирать как параметр ФП — это закрепляет провалившийся костыль,
   а не первичное противоречие.

1. Перебери 2–3 кандидата параметра на РАЗНЫХ уровнях системы:
   • геометрия ключевых элементов, остающихся в системе (форма и диаметр торца, взаимное расположение волокон,
     размер зоны сведения, ширина зоны объединения…);
   • процесс (режим сплавления, угол сходимости пучка…);
   • свойство среды/поля (когерентность, распределение интенсивности…).
   НЕ среди характеристик отвергнутого компонента.

2. Для каждого кандидата спроси:
   «Изменение этого параметра напрямую усиливает или ослабляет root_cause?»
   Отбрось параметры, не связанные с механизмом root_cause.

3. Проверка «перестройка системы vs оптимизация по краям»:
   • если параметр требует нового изделия/оборудования/материала — это перестройка;
   • если параметр меняется режимом, геометрией или использованием имеющихся ресурсов — оптимизация.
   Приоритет — параметр из root_cause, а не постороннее свойство (цвет, вкус, эстетика).

4. Выбери ОДИН параметр с наивысшей связью с root_cause.
   Параметр должен относиться к элементу, который остаётся в ИКР (напр. торец объединённого световода, зона сведения),
   а не к отвергнутому компоненту."""

FP_RESOLUTION_AXES = """Алгоритм выбора ОСИ РАЗРЕШЕНИЯ (выполни ПОСЛЕ выбора параметра P, ДО записи ФП):

0. Проверка фазности задачи:
   Прочитай условие, НЭ «когда», unrealized_ideas, known_solutions.
   Признаки разных фаз процесса: «при инкубации»/«в эксперименте», «сначала»/«потом», «на этапе»,
   подготовка/работа, загрузка/обработка, разные требования к одному параметру на разных этапах.
   Если фазность есть — ЗАПРЕЩЕНО статическое ФП («одновременно X и anti-X»); приоритет — ось ВРЕМЕНИ.

1. Перебери 4 оси разделения противоречивых требований для выбранного параметра P:

   • ВО ВРЕМЕНИ: может ли P иметь значение X в одной фазе/моменте и anti-X в другой?
     (подготовка/работа, инкубация/эксперимент, загрузка/обработка)
   • В ПРОСТРАНСТВЕ: X в одной части системы, anti-X в другой?
   • В СТРУКТУРЕ: X на макроуровне, anti-X на микро?
   • В НАДСИСТЕМЕ: вынести одно из требований на уровень выше?

2. Выбери ось с наивысшим приоритетом по задаче:
   — при фазности процесса → ВРЕМЯ (формула B с явными фазами [A] и [B]);
   — иначе выбери ось, которая лучше открывает пространство решений в рамках constraints и unrealized_ideas.

3. Запиши ФП по формуле A (статическая) или B (фазовая). При формуле B назови фазы дословно из брифа."""

FP_RETRY_SYSTEM_PROMPT = """Ты — старший TRIZ-аналитик. Переформулируй ПСА (root_cause, causal_chains) и противоречия (ТП, ФП).

Все формулировки — на русском языке.

ПСА — если в замечаниях валидатора указано, что root_cause зафиксирован на костыле:
- перестрой цепочку «почему?» до ПЕРВИЧНОЙ геометрической/физической причины;
- если в цепочке фигурирует компонент текущего решения (линзы, клей…) — спроси «зачем он нужен?» и копай дальше;
- root_cause НЕ должен содержать отвергнутый в ИКР/constraints компонент.

Техническое противоречие (ТП): конфликт улучшения одного параметра и ухудшения другого.

Физическое противоречие (ФП) — строгие правила:

1. ФП = ОДИН параметр ОДНОГО элемента, который должен иметь два ВЗАИМОИСКЛЮЧАЮЩИХ значения (X и анти-X).

2. ЗАПРЕЩЕНО маскировать пространственное разделение под ФП:
   «внутри/снаружи», «сверху/снизу», «на внешней/внутренней поверхности»,
   «с одной стороны/с другой», «в зоне A/в зоне B» как два требования к одному параметру В ОДИН МОМЕНТ.
   Это не ФП, а принцип разделения в пространстве.
   ИСКЛЮЧЕНИЕ — разделение во ВРЕМЕНИ: если в задаче разные фазы процесса, формулируй
   «на фазе [A] должен быть X… и на фазе [B] должен быть anti-X» (инкубация/эксперимент и т.п.).
   ЗАПРЕЩЕНО при фазности брифа статическое ФП без указания фаз («одновременно широкий и узкий»).

3. Алгоритм построения ФП:
   a) Выбери ОДИН элемент и ОДИН его параметр, который корень конфликта объясняет через root_cause.
   b) Запиши ФП одним предложением строго по формуле: «[элемент]: параметр [P] должен быть [X], чтобы [A], и должен быть [анти-X], чтобы [B]».
   c) [P] — полное имя параметра с привязкой к элементу (не абстрактное «влажность», а «влажность поверхности стакана»).
   d) [A] и [B] — разные полезные функции: зачем нужен X и зачем нужен анти-X в контексте задачи.
   e) ОБА значения [X] и [анти-X] сами по себе желательны для системы — конфликт двух польз, а не «польза против абсурда»
   (нельзя: «высокие потери, чтобы избежать линз», «низкая прочность, чтобы сэкономить материал»).

4. Чек-лист валидатора (все пункты обязательны):
   - single_parameter: один элемент + один именованный параметр;
   - dual_requirement: явно «должен быть [X]» и «и должен быть [анти-X]»;
   - useful_functions_justified: после каждого «чтобы» — конкретная полезная функция, не общие слова;
   - both_values_inherently_desirable: оба значения параметра по-своему полезны, ни одна половина — не заведомо вредное состояние.

5. Замечания валидатора (feedback) — исправь буквально каждое; не повторяй отклонённую формулировку и не маскируй ту же ошибку другими словами.

6. Учитывай контекст тупиков (known_solutions, why_failed) и доступные ресурсы — ФП должно открывать пространство решений в рамках constraints, а не подталкивать к уже провалившимся подходам.

""" + FP_PARAMETER_ENUMERATION + """

""" + FP_RESOLUTION_AXES + """

ТП и ФП ОБЯЗАНЫ опираться на root_cause (первичная причина), а не на симптом НЭ и не на отвергнутый костыль.
Верни root_cause, causal_chains, technical_contradiction, physical_contradiction. Не генерируй остальные разделы отчёта."""

FP_RETRY_USER_PROMPT = """Задача:
{problem}

Описание системы (контекст):
{problem_description}

Корневая причина (физическое/химическое явление по ПСА):
{root_cause}

Причинно-следственная цепочка:
{causal_chains}

Доступные ресурсы:
{resources}

Известные попытки решения (тупики — не подталкивай ФП к тем же подходам):
{known_solutions}

Почему не сработало:
{why_failed}

Нереализованные идеи (контекст — не дублируй как готовое ФП, но учитывай при выборе параметра):
{unrealized_ideas}

ИКР (ideal_final_result):
{ideal_final_result}

Ограничения (constraints):
{constraints}

Текущее техническое противоречие (ТП):
{technical_contradiction}

Текущее физическое противоречие (ФП), ОТКЛОНЁННОЕ валидатором (НЕ копируй дословно — исправь ошибки):
{physical_contradiction}

ЗАМЕЧАНИЯ ВАЛИДАТОРА ФП (обязательно устрани каждое; не повторяй отклонённую формулировку):
{feedback}

{fp_formula}

Переформулируй root_cause, causal_chains, ТП и ФП от первичной причины (не от симптома и не от костыля).
root_cause и causal_chains НЕ ДОЛЖНЫ содержать названия компонентов, отвергнутых в ИКР/constraints (напр. «линз», если ИКР «без линз»).

ФП — одно предложение по формуле выше. Обязательно:
- сначала мысленно перебери 2–3 кандидата параметра (см. алгоритм выбора P) и выбери связанный с root_cause;
- затем перебери 4 оси разрешения (см. алгоритм осей); при фазности в брифе — формула B с явными фазами;
- начни с «[элемент]: параметр [полное имя параметра]»;
- дважды используй «должен быть» (муж. род, т.к. «параметр») с противоположными значениями;
- после каждого «чтобы» — конкретная полезная функция из контекста задачи;
- устрани ВСЕ пункты из замечаний валидатора.
Не маскируй пространственное разделение в один момент; при разных фазах процесса — только временное разделение."""

PSA_ROOT_RETRY_SYSTEM = """Ты — старший TRIZ-аналитик. Переформулируй ТОЛЬКО root_cause и causal_chains (ПСА).

root_cause = первичная геометрическая или физическая причина, НЕ дефект текущего костыля (линз, клея, конкретного механизма).
Если компонент отвергнут в ИКР/constraints — ЗАПРЕЩЕНО упоминать его в root_cause и в конце causal_chains.
Цепочка «почему?» — 4–5 уровней до первичной причины. Если встречается компонент текущего решения — спроси «зачем он нужен?» и копай дальше.
Все формулировки — на русском."""

PSA_ROOT_RETRY_USER = """Задача:
{problem}

ИКР:
{ideal_final_result}

Ограничения:
{constraints}

Текущий root_cause (НЕВЕРНЫЙ — перепиши):
{root_cause}

Текущая causal_chains (перепиши):
{causal_chains}

ЗАМЕЧАНИЯ ВАЛИДАТОРА:
{feedback}

Верни root_cause и causal_chains. Не упоминай отвергнутые компоненты."""
